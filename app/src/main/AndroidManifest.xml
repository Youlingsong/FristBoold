<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.jianghuo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Denglu_Activity"

            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Zhuce_Activity"></activity>
        <activity android:name=".Shouye_Activity"
            android:label="           OutLine"></activity>
        <activity android:name=".ShowCountry_Activity" android:label="           切换线路"></activity>
        <activity android:name=".Activity_taocan"  android:label="           套餐购买"></activity>
        <activity android:name=".Activity_service"></activity>
    </application>
    <uses-permission android:name="android.permission.RESTART_PACKAGES\"></uses-permission>


</manifest>